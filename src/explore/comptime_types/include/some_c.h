



enum wyliczanka{
    ene,
    due,
    rike,
    fake,
};

struct sampleStruct {
    char a;
    char b;
};

void do_something(struct sampleStruct *ss);
void do_something_else(struct sampleStruct *ss);